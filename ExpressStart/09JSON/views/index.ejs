<!DOCTYPE html>
<html>
  <head>
    <title>Taco Recipes</title>
    <link rel="stylesheet" type="text/css" href="/styles/main.css" />
  </head>

  <body>
    <h1>Taco Town 🌮</h1>

    <form action="/recipe" method="POST" class="buttons">
      <button type="submit" value="Chicken" name="choice">🍗</button>
      <button type="submit" value="Beef" name="choice">🥩</button>
      <button type="submit" value="Fish" name="choice">🐟</button>
    </form>
    <div id="recipeContainer" class="hidden">
      <% if (!locals.recipe) { %>
      <h2>Pick your favourite taco ingredient 👆</h2>
      <% } else{ %>

      <h2><%= recipe.name %></h2>
      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li>
          <%= locals.recipe?.ingredients.protein.name %>, <%=
          locals.recipe?.ingredients.protein.preparation %>
        </li>
        <li><%= locals.recipe?.ingredients.salsa.name %></li>
        <% locals.recipe?.ingredients.toppings.forEach(topItem => {%>
        <li><%= topItem.quantity %> <%= topItem.name %> <% }); %></li>
      </ul>

      <%}%>
    </div>
  </body>
</html>
